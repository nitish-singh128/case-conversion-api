name: C++ CI ProcessStringDLL - All Platforms

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-dll:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      # -------------------------------
      # Checkout repository
      # -------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------------
      # Install dependencies
      # -------------------------------
      - name: Install compiler & CMake (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y build-essential cmake

      - name: Install CMake (macOS)
        if: runner.os == 'macOS'
        run: brew install cmake

      - name: Install CMake (Windows)
        if: runner.os == 'Windows'
        run: choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' --yes

      # -------------------------------
      # Clean build folder
      # -------------------------------
      - name: Clean build folder
        if: runner.os != 'Windows'
        run: rm -rf CaseConversionAPI/CppLib/build_dll

      - name: Clean build directory (Windows)
        if: runner.os == 'Windows'
        run: |
          Remove-Item -Recurse -Force "CaseConversionAPI\CppLib\build_dll"

      # -------------------------------
      # Configure DLL project
      # -------------------------------
      - name: Configure DLL project
        run: cmake -S CaseConversionAPI/CppLib -B CaseConversionAPI/CppLib/build_dll -DCMAKE_BUILD_TYPE=Release

      # -------------------------------
      # Build DLL
      # -------------------------------
      - name: Build DLL
        run: cmake --build CaseConversionAPI/CppLib/build_dll --config Release --parallel

      # -------------------------------
      # Upload DLL artifact
      # -------------------------------
      - name: Upload DLL
        uses: actions/upload-artifact@v4
        with:
          name: ProcessStringDLL-${{ matrix.os }}
          path: CaseConversionAPI/CppLib/build_dll/lib/